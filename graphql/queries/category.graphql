#import "../fragments/categoryFields.graphql"
query Category($id: ID!) {
  category(id: $id) {
    ...CategoryFields
    banner_img_en
    banner_link_en
    products {
      id
      title
      price
      image
      alt_image
    }
  }
}